/* eslint-disable no-console */
'use strict';

const { speakWithUser } = require('./speakWithUser.js');

console.log(
  'Remember that you should enter 4 digits from'
  + '1 to 9, all numbers must have unique equals'
);

speakWithUser();
